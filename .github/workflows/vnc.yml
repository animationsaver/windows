name: macOS Screen Sharing via Tailscale

on:
  schedule:
    - cron: "27 */6 * * *"
  workflow_dispatch:
    inputs:
      session_minutes:
        description: "Minutes to keep the session alive (max ~350 / 6h limit)"
        required: false
        default: "350"
  repository_dispatch:
    types: [start-vnc]

jobs:
  vnc:
    runs-on: macos-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create dedicated VNC admin user 'vncuser'
        env:
          ACCOUNT_PW: ${{ secrets.RDP_PASSWORD }}
        run: |
          set -e
          PW="${ACCOUNT_PW:-changeme1234}"
          sudo sysadminctl -addUser vncuser -fullName "VNC User" -password "$PW" -admin
          sudo createhomedir -c -u vncuser >/dev/null 2>&1 || true
          echo "Created user 'vncuser'."

      # Proven approach (dikeckaan/MacOS-Workflow-VNC, paulz gist):
      # enable Remote Management (ARD) with full privileges for ALL users AND
      # legacy VNC, then write the obfuscated VNC password directly into
      # com.apple.VNCSettings.txt. Finder's Screen Sharing then connects with
      # just the VNC password. Do NOT deactivate RM / hand-load screensharingd
      # (that causes the "Screen Sharing is not permitted" conflict).
      - name: Enable Remote Management + legacy VNC password
        env:
          VNC_LEGACY_PW: ${{ secrets.VNC_PASSWORD }}
          FALLBACK_PW: ${{ secrets.RDP_PASSWORD }}
        run: |
          set +e
          KICK="/System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart"

          PW="${VNC_LEGACY_PW:-}"
          if [ -z "$PW" ]; then PW="$(printf '%s' "$FALLBACK_PW" | cut -c1-8)"; fi

          # Grant full-control access to all users and turn on legacy VNC.
          sudo "$KICK" -configure -allowAccessFor -allUsers -privs -all
          sudo "$KICK" -configure -clientopts -setvnclegacy -vnclegacy yes

          # Write the (max 8-char) VNC password in the obfuscated format ARD expects.
          HASH=$(python3 -c 'import sys; k=bytes.fromhex("1734516E8BA8C5E2FF1C39567390ADCA"); p=sys.argv[1][:8].encode(); print("".join("%02X"%(k[i]^(p[i] if i<len(p) else 0)) for i in range(len(k))))' "$PW")
          echo "$HASH" | sudo tee /Library/Preferences/com.apple.VNCSettings.txt >/dev/null

          # Restart the ARD agent and activate Remote Management.
          sudo "$KICK" -restart -agent -console
          sudo "$KICK" -activate
          echo "Remote Management + legacy VNC enabled."
          echo "Connect from Finder with the VNC password (no account needed)."

      - name: Install Tailscale
        run: |
          brew install tailscale
          echo "Tailscale installed"

      - name: Remove stale Tailscale device
        env:
          TS_API_KEY: ${{ secrets.TS_API_KEY }}
        run: |
          if [ -z "$TS_API_KEY" ]; then
            echo "TS_API_KEY not set; skipping stale-device cleanup"
          else
            ids=$(curl -s -H "Authorization: Bearer $TS_API_KEY" \
              "https://api.tailscale.com/api/v2/tailnet/-/devices" \
              | jq -r '.devices[] | select((.hostname // "" | startswith("gha-macos-vnc")) or (.name // "" | startswith("gha-macos-vnc"))) | .id')
            for id in $ids; do
              curl -s -X DELETE -H "Authorization: Bearer $TS_API_KEY" \
                "https://api.tailscale.com/api/v2/device/$id" >/dev/null
              echo "Deleted stale device: $id"
            done
          fi

      - name: Connect Tailscale
        run: |
          sudo tailscaled > /tmp/tailscaled.log 2>&1 &
          sleep 5
          sudo tailscale up --authkey="${{ secrets.TS_AUTHKEY }}" \
            --hostname="gha-macos-vnc" --accept-routes
          sleep 3
          IP=$(tailscale ip -4)
          echo "=================================================="
          echo "  Tailscale IP : $IP"
          echo "  From a Mac   : Finder > Go > Connect to Server > vnc://$IP"
          echo "  Password     : the VNC password (VNC_PASSWORD, or the first"
          echo "                 8 chars of RDP_PASSWORD if VNC_PASSWORD is unset)."
          echo "=================================================="

      - name: Provision Chrome environment (optional)
        run: |
          if [ -f setup.sh ]; then
            chmod +x setup.sh
            ./setup.sh || echo "setup.sh failed (non-fatal)"
          else
            echo "setup.sh not found; skipping"
          fi

      - name: Add Japanese input source (best-effort)
        run: |
          defaults write com.apple.HIToolbox AppleEnabledInputSources -array-add \
            '{ "InputSourceKind" = "Keyboard Layout"; "KeyboardLayout ID" = 0; "KeyboardLayout Name" = "U.S."; }' \
            '{ "Bundle ID" = "com.apple.inputmethod.Kotoeri"; "Input Mode" = "com.apple.inputmethod.Japanese"; "InputSourceKind" = "Input Mode"; }' 2>/dev/null || true
          echo "Japanese (Kotoeri) input requested."

      - name: Keep session alive
        env:
          SESSION_MINUTES: "${{ github.event.inputs.session_minutes || github.event.client_payload.minutes || '350' }}"
        run: |
          MIN="${SESSION_MINUTES:-350}"
          case "$MIN" in ''|*[!0-9]*) MIN=60;; esac
          if [ "$MIN" -le 0 ]; then MIN=60; fi
          echo "Keeping session for $MIN minutes"
          echo "Create /Users/runner/Desktop/stop.txt to end early"
          END=$(( $(date +%s) + MIN * 60 ))
          while [ "$(date +%s)" -lt "$END" ]; do
            if [ -f "/Users/runner/Desktop/stop.txt" ] || [ -f "/Users/vncuser/Desktop/stop.txt" ]; then
              echo "stop.txt detected. Ending."
              break
            fi
            sleep 30
          done

      - name: Tailscale logout and remove device
        if: always()
        env:
          TS_API_KEY: ${{ secrets.TS_API_KEY }}
        run: |
          sudo tailscale logout || echo "logout skipped"
          if [ -n "$TS_API_KEY" ]; then
            ids=$(curl -s -H "Authorization: Bearer $TS_API_KEY" \
              "https://api.tailscale.com/api/v2/tailnet/-/devices" \
              | jq -r '.devices[] | select((.hostname // "" | startswith("gha-macos-vnc")) or (.name // "" | startswith("gha-macos-vnc"))) | .id')
            for id in $ids; do
              curl -s -X DELETE -H "Authorization: Bearer $TS_API_KEY" \
                "https://api.tailscale.com/api/v2/device/$id" >/dev/null
              echo "Removed device from tailnet: $id"
            done
          fi
